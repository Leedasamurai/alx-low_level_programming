#include "main.h"
/**
 * _abs - Computes the absolute value of an integer.
 *
 * @i: input as an integer
 *
 * Return: Absoulute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
